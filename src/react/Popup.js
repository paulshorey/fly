import * as React from 'react';
import EditOptions from './EditOptions';

export const Popup = ({}) => {
  // edit messages
  return (
    <div>
      <EditOptions />
    </div>
  );
};

export default Popup;
